import express from "express";

const router = express.Router();
router.use(express.urlencoded({ extended: true }));

router.post("/quan", async function (req, res) {
  try {
  } catch (error) {}
});

export default router;
